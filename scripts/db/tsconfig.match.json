{
  "//": "Standalone rather than extending the app tsconfig: expo's base sets customConditions, which is incompatible with the commonjs+node resolution these scripts need. Only matching.ts and its transitive deps are compiled; every non-relative import in that graph is `import type`, so nothing here has to resolve @/ at runtime.",
  "compilerOptions": {
    "outDir": "../../.tmp-match",
    "rootDir": "../..",
    "module": "commonjs",
    "target": "es2022",
    "moduleResolution": "node",
    "skipLibCheck": true,
    "esModuleInterop": true,
    "baseUrl": "../..",
    "paths": { "@/*": ["./*"] }
  },
  "include": ["../../features/challenges/matching.ts"]
}
